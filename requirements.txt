# Backend
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pillow>=10.2.0
pydantic>=2.6.0
httpx>=0.27.0

# Computer Vision — object detection (Req04, Req06)
ultralytics>=8.1.0         # YOLOv8n; auto-downloads yolov8n.pt on first run

# Material classification (Req05)
transformers>=4.40.0       # CLIP zero-shot pipeline
torch>=2.2.0               # CPU-only inference (NfReq13)
torchvision>=0.17.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0              # TestClient
